{
  "functions": {
    "api/**/*.py": {
      "runtime": "python3.12",
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api" }
  ],
  "buildCommand": "pip install -r api/requirements.txt"
}
